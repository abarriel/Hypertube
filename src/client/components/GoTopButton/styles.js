import styled from 'styled-components';

import { MAIN_COLOR } from '../../colors';

export const GoTopButtonContainer = styled.div`
  position:fixed;
  display:flex;
  width:30px;
  height:30px;
  border-radius: 100%;
  backgrkound-color: ${MAIN_COLOR};
`;

