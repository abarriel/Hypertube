import React from 'react';

import {
  LostContainer,
  FormContainer,
  Header,
  Logo,
} from './styles';

const Lost = () => (
  <LostContainer>
    <Header>
      <Logo />
    </Header>
    <FormContainer>

    </FormContainer>
  </LostContainer>
);

export default Lost;
