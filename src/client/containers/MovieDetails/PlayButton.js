import React from 'react';

import {
  PlayButtonContainer,
} from './styles';

const PlayButton = () => (
  <PlayButtonContainer to="/login" >
    Lecture
  </PlayButtonContainer>
);

export default PlayButton;
